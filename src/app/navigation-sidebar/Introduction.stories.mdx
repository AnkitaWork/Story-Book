import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="NavigationSidebar/ introduction" />

# Welcome to [**Navigation Sidebar**](https://www.npmjs.com/package/ng-sidebar)

 An Angular sidebar component Formerly called ng2-sidebar

 In your app component, simply use add a `<ng-sidebar-container>` wrapper, then place your `<ng-sidebar>`(s) and content within it. Your page content should be in some container with a `ng-sidebar-content` attribute.

[**Demo**](https://echeung.me/ng-sidebar/)

Install `npm i ng-sidebar`
